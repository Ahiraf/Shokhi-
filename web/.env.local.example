# Copy to .env.local for local dev. On Vercel, set this in Project → Settings → Env Vars.
# This is the URL of your Python backend (Render). Locally it's the uvicorn server.
NEXT_PUBLIC_API_URL=http://localhost:8000
