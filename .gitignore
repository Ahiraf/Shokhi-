# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# --- Secrets (NEVER commit your API key) ---
.streamlit/secrets.toml
.env
*.key

# --- Training datasets (licensed; re-download with api/fetch_datasets.py) ---
api/data/datasets/

# NOTE: api/data/models/*.joblib ARE committed on purpose — small, and the deployed
# app needs them for the risk signal. Retrain anytime with train_risk_models.py.

# --- Node / Next.js (also has web/.gitignore) ---
node_modules/
.next/
.env.local

# --- OS / editor ---
.DS_Store
.idea/
.vscode/
*.log
