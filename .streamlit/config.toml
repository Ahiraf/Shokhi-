# Shokhi — Streamlit theme & server config (safe to commit; no secrets here).
[theme]
primaryColor = "#b5175f"
backgroundColor = "#fff5f9"
secondaryBackgroundColor = "#fce7f0"
textColor = "#3a2230"
font = "sans serif"

[server]
headless = true
enableCORS = false
enableXsrfProtection = true
