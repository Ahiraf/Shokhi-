# Core UI — enough to run the offline (mock) demo.
streamlit>=1.30

# Real Gemma 4 backend via Ollama (local, offline download). SHOKHI_BACKEND=ollama.
requests>=2.31

# Real Gemma 4 backend via Google AI Studio (hosted, API key — no download).
# Needed only when SHOKHI_BACKEND=gemini. Get a free key at aistudio.google.com.
google-genai>=0.3

# Optional ML support signal — the PCOS / endometriosis risk classifiers.
# Needed to TRAIN (train_risk_models.py) and to load the models at runtime.
# The app runs fine without these (the risk signal simply turns off).
scikit-learn>=1.3
pandas>=2.0
joblib>=1.3
openpyxl>=3.1   # to read the endometriosis .xlsx dataset during training

# Optional: Bangla text-to-speech so guidance can be HEARD (accessibility / low-literacy).
# Needs internet. Uncomment to enable the "🔊 শুনতে চাই" button.
# gTTS>=2.5

# Roadmap (not required for the demo):
#   Bangla speech-to-text for the voice/IVR path — e.g. openai-whisper or a hosted STT.
#   These are NON-generative speech tools (allowed as support for Gemma 4).
