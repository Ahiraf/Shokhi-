# --- Shokhi backend (FastAPI on Render) ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
python-multipart>=0.0.9      # for audio file uploads

# Real Gemma 4 backend via Google AI Studio (hosted, API key). SHOKHI_BACKEND=gemini.
google-genai>=0.3

# Real Gemma 4 backend via Ollama (local). SHOKHI_BACKEND=ollama.
requests>=2.31

# ML support signal — PCOS / endometriosis risk classifiers.
scikit-learn>=1.3
pandas>=2.0
joblib>=1.3
openpyxl>=3.1               # only needed to (re)train from the .xlsx dataset
